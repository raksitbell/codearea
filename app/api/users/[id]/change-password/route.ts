import { NextResponse } from "next/server"; import { currentUser } from "@/server/auth/request"; import { changeOwnPassword } from "@/server/auth/module"; import { jsonError } from "@/server/http"; import { DomainError } from "@/server/domain/errors";
export async function PUT(request:Request,{params}:{params:Promise<{id:string}>}){try{const actor=await currentUser();if(actor.id!==Number((await params).id))throw new DomainError("FORBIDDEN","เปลี่ยนได้เฉพาะรหัสผ่านของตนเอง");await changeOwnPassword(await request.json(),actor);return NextResponse.json({message:"เปลี่ยนรหัสผ่านแล้ว"});}catch(error){return jsonError(error);}}
